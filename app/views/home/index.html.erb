<h1>Home#index</h1>
<p>Find me in app/views/home/index.html.erb</p>
<div class='container'>
	<% @rooms.each do |room| %>
		<p>
			<%= link_to room.title, chatroom_path(room) %>
		</p>
	<% end %>
</div>